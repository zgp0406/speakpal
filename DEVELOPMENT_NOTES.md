# 开发记录

## 当前进度

- 已完成 Android 项目骨架搭建
- 已完成页面路由串联
- 已完成首页、单词列表页、练习页、结果页、记录页占位
- 已加入示例单词数据
- 已使用系统 TTS 实现标准单词发音
- 已实现麦克风权限、真实录音、本地保存和回放
- 已将录音升级为讯飞兼容的 16kHz、16bit、单声道 WAV
- 已建立语音识别、发音评测及串行业务管线接口
- 已实现稳定的离线模拟评测数据结构
- 已实现六类重点音素的口型与舌位纠错提示
- 已实现练习记录本地持久化和录音回放

## 2026 年 7 月 3 日实现

- 录音文件保存于 App 私有目录 `files/recordings`
- 新录音以 WAV 保存，内部音频为 16kHz 单声道 PCM
- 练习记录使用 SharedPreferences JSON 保存，最多保留 100 条
- 当前语音识别实现会校验 WAV 格式并返回离线演示文本，结果页明确标记“离线演示”
- 评测器按单词返回稳定结果，便于离线答辩演示
- 结果页展示准确度、流利度、完整度、错误音素和舌位示意
- 真实在线评测、3D 数字人和大模型推荐仍作为后续模块

## 2026 年 6 月任务

- 对照开题报告继续完善需求分析、技术路线和模块拆分
- 保持当前 Android 原型可运行，作为后续功能接入基础
- 暂不接入真实语音评测接口，先使用模拟评分和本地演示数据

## 路由结构

- `home`
- `words`
- `practice/{wordId}`
- `result/{wordId}/{score}`
- `records`

## 后续开发顺序

1. 接入讯飞 IAT 语音听写，替换 `LocalDemoSpeechRecognitionService`
2. 接入 ISE 发音评测，替换 `LocalDemoPronunciationEvaluationService`
3. 用接口结果替换本地模拟音素诊断
4. 增加数字人口型动画
5. 接入大模型个性化训练推荐
6. 用 Room 数据库替换 SharedPreferences
7. 增加自动化测试和多设备兼容性验证

## 说明

- `README.md` 只用于写项目介绍
- 本文件用于记录开发过程、任务拆分和后续实现计划
